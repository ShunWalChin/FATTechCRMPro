import type {Metadata} from 'next';
import {Proposals} from '@/components/commercial';
export const metadata:Metadata={title:'Propostas · FAT Tech CRM'};
export default function Page(){return <Proposals/>}
