import type {Metadata} from 'next';
import {SalesGoals} from '@/components/commercial';
export const metadata:Metadata={title:'Metas · FAT Tech CRM'};
export default function Page(){return <SalesGoals/>}
